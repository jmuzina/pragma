<!-- <%= generatorPackageName %> <%= generatorPackageVersion %> -->

<script module lang="ts">
  import { defineMeta } from "@storybook/addon-svelte-csf";
  import <%= componentName %> from "./<%= componentName %>.svelte";

  /*
    CSF3 story
    Uses the latest Storybook format for Svelte.
  */
  const { Story } = defineMeta({
    title: "<%= componentName %>",
    tags: ["autodocs"],
    component: <%= componentName %>,
  });
</script>

<!-- More on writing stories with args: https://storybook.js.org/docs/writing-stories/args -->
<Story name="Default"><%= componentName %></Story>

<Story
  name="With Props"
  args={{
    class: "custom-class",
    id: "unique-id",
    style: "background-color: tomato; padding: 10px; color: white;",
  }}
>
  <%= componentName %> with props
</Story>
