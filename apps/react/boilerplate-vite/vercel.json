{
  "builds": [
    {
      "src": "api/server/renderer.ts",
      "use": "@vercel/node",
      "config": {
        "installCommand": "bun install"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/server/renderer.ts"
    }
  ]
}