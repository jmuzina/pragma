/** Name of the `ExampleControlField.name` before it was transformed to a RHF key. */
export const ORIGINAL_VAR_NAME_KEY = "data-original-var-name";

export const TYPO_METRICS_MAP: Record<
  string,
  { ascender: number; descender: number; unitsPerEm: number }
> = {
  "Times New Roman": {
    ascender: 1825,
    descender: -443,
    unitsPerEm: 2048,
  },
};
